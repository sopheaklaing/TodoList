/**
 * Application constants.
 * Centralizes keys and default values for easier maintenance and refactoring.
 */

export const STORAGE_KEYS = {
  PLANS: 'everyplan_plans',
  SELECTED_PLAN_ID: 'everyplan_selected_plan_id',
}

export const DEFAULTS = {
  PLAN_NAME: 'New plan',
  TODO_NAME: 'New todo',
}
